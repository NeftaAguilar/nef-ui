# nef-design-system

A small, accessible React component library built on Radix Primitives,
designed so the primitive layer can be swapped for Base UI later.

Status: pre-scaffold. Stack decisions are being settled in CONTEXT.md and docs/adr/
before any tooling lands.
