import { Canvas, Controls, Meta, Primary } from '@storybook/addon-docs/blocks';
import * as HStackStories from './HStack.stories';

<Meta of={HStackStories} />

# HStack

A `display: flex; flex-direction: row` wrapper — nothing else. It ships no
`gap`, alignment, padding or margin, on purpose: spacing between children
differs by usage, so it stays out of the Recipe and lives in the consumer's own
`className` or `style`, the same way every other Component here accepts and
merges `className` instead of inventing spacing props.

<Primary />

<Controls />

## Without a gap

Left alone, children sit flush against each other — there is no default spacing
to override.

<Canvas of={HStackStories.WithoutGap} />

## Adding spacing

Add `gap` (or margin/padding) the same way you would to any `div` — via
`style` or your own class.

<Canvas of={HStackStories.Playground} />
